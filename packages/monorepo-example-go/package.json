{
  "name": "@opsgang/monorepo-example-go",
  "version": "1.0.0",
  "description": "Monorepo example package for Go",
  "scripts": {
    "build": "npm run build:docker",
    "build:docker": "./scripts/build.sh",
    "publish": "npm run build",
    "deploy": "./scripts/deploy.sh",
    "lint": "eslint .",
    "precommit": "lint-staged",
    "precommit:lint": "eslint",
    "precommit:lint:json": "jsonlint"
  },
  "precommit": [
    "precommit"
  ],
  "lint-staged": {
    "**/*.(js|jsx)": [
      "npm run precommit:lint",
      "npm test"
    ],
    "**/*.json": [
      "npm run precommit:lint:json"
    ]
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
  }
}
